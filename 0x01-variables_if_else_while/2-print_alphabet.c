#include <stdio.h>
/**
 * main - Write a program that prints the alphabet in lowercase
 *
 * Return: 0 if it exist
 */
int main(void)
{
	char ch;

	for (ch = 'a'; ch <= 'z'; ch++)
	{
		putchar(ch);
	}
	putchar('\n');
return (0);
}
